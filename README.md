## Getting Started
1. Create an environment variable in .env file, NEXT_PUBLIC_BACKEND_URL and provide the url of your backend
2. Run the development server:

```bash
npm run dev
# or
yarn dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.